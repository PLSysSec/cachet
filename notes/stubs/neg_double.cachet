import "../cacheir.cachet"

ir CacheStub emits CacheIR {
    op Neg(
        inputId: ValueId,
        valId: NumberId,
    ) {
        emit CacheIR::GuardIsNumber(inputId, valId);
        emit CacheIR::DoubleNegationResult(valId);
    }
}
