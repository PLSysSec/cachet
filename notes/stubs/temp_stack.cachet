import "../cacheir.cachet"
import "../utils.cachet"

ir CacheStub emits CacheIR {
    op temp_stack(
    ) {
        initRegAllocator();
        initValueOutput();

        emit CacheIR::PushPopRegs(); 
    }
}
