import "../cacheir.cachet"

ir CacheStub emits CacheIR {
    op Not(
        inputValueId: ValueId,
    ) {
        CacheIR::initValueInput(inputValueId);
        CacheIR::defineValueReg(inputValueId);

        emit CacheIR::GuardToInt32(inputValueId);
        let inputInt32Id = ValueId::toInt32Id(inputValueId);
        emit CacheIR::Int32NotResult(inputInt32Id);
    }
}
