fn ceil_spec(r: Range, x: Double) {
    assume Range::well_formed(r);
    assume Range::in_range(r, x);

    let r2 = Range::ceil(r);
    let x2 = JSValue::ceil(x);

    assert Range::well_formed(r2);
    assert Range::in_range(r2, x2);
}

